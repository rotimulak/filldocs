# Технический долг

## Высокий приоритет

### Тестирование backend (pytest)
- [ ] Тесты для `DocxFiller.extract()` — извлечение реквизитов
- [ ] Тесты для `DocxFiller.fill()` — заполнение документов
- [ ] Тесты API endpoints (`/api/extract-requisites`, `/api/fill`)
- [ ] Тесты валидации (ИНН, КПП, ОГРН, БИК, счета)
- [ ] Тесты конвертации .doc → .docx

## Средний приоритет

### Тестирование frontend (vitest)
- [ ] Тесты компонентов (DropZone, JsonPreview, RequisitesPanel, FillPanel)
- [ ] Тесты hooks (useLocalStorage)
- [ ] Тесты API client

### Детальный FillReport
- [ ] Показывать список заполненных полей с значениями
- [ ] Показывать пропущенные поля (нет значения в реквизитах)
- [ ] Показывать ошибки (ячейка уже заполнена)
- [ ] Обновить backend `/api/fill` для возврата детального отчёта

## Низкий приоритет

### Сохранение стиля при заполнении
- [ ] Копировать шрифт из соседней ячейки
- [ ] Копировать размер шрифта
- [ ] Копировать выравнивание
- [ ] Реализовать `copy_style()` в `DocxFiller`

### Docker compose
- [ ] Dockerfile для backend
- [ ] Dockerfile для frontend (nginx)
- [ ] docker-compose.yml
- [ ] Документация по деплою

### Прочее
- [ ] Логирование операций на backend
- [ ] Rate limiting для API
- [ ] Метрики (время обработки, размер файлов)
